# Service Mesh

A Service Mesh is a dedicated infrastructure layer for handling communication between services.

In a Service Mesh, each service has a sidecar proxy that handles all of the services networking concerns (retries, timeouts, encryption, load balancing, observability).

Services still call other services by URL.
