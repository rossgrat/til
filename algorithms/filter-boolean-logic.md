# Filter Boolean Logic
When designing "contains" filters, thinking about the boolean logic is important. There are a few different levels:
- ORing like fields, ANDing different fields
- AND / OR groups
- Boolean logic
- Full nested boolean logix

