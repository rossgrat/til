# CLI Tools for databases

- `psql` for Postgres
- `mongosh` for DocumentDB
- `rpk` for Kafka
- DynamoDB has no REPL, and queries are made directly through `aws`
- `redis-cli` for Redis/Valkey
- Opensearch requests are made using standard HTTP and `curl`

